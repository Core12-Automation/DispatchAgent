# /etc/logrotate.d/ai-dispatcher
# Install: sudo cp deploy/logrotate.conf /etc/logrotate.d/ai-dispatcher
#
# Rotates daily, keeps 14 compressed copies (~2 weeks).
# The postrotate signal causes the service to re-open its log file handle
# so no log lines are lost between rotation and the next write.

/var/log/ai-dispatcher/app.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    create 0640 dispatcher dispatcher
    postrotate
        # SIGHUP tells the Python logging FileHandler to reopen the file
        systemctl kill --signal=HUP ai-dispatcher.service 2>/dev/null || true
    endscript
}
